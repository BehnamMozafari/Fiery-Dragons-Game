package game.view;

public interface SetBoardStrategy {

  /**
   * Sets up the board to display chit cards, volcano cards.
   *
   * @param board The game board to set.
   */
  void setUpBoard(GameBoard board);
}
